namespace ChargePoint.CarManagement.Domain.Constants
{
    public class Messages
    {
        public const string SuccessMessage = nameof(SuccessMessage);
        public const string ErrorMessage = nameof(ErrorMessage);
    }
}
